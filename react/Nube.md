Para publicar proyectos en la nube se puede usar **Netlify**.

Después, en el proyecto:
Sí se usó `npm` para crear.
```ps
npm run build
```
Esto crea una carpeta llamada *build*, esta carpeta es la que se sube a github o a Netlify.
